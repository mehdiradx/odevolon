import {StyleSheet} from 'react-native';
import {gray} from '../../../constants/variables';

export default StyleSheet.create({
  wrappert: {
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 100,
  },
  text: {
    color: gray,
  },
});
