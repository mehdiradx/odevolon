import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  wrapper: {
    flexDirection: 'row',
  },
  image: {
    resizeMode: 'cover',
    width: 30,
    height: 22,
    marginLeft: 10,
  },
});
